process {
    executor = 'local'   // Lifebit usually overrides this to their HPC environment
    cpus = 1
    memory = '1 GB'
    time = '1h'
}

profiles {
    lifebit {
        process.executor = 'local'
    }
}
